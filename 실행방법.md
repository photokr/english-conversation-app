# 영어 회화 마스터 앱 — 실행 방법

## 처음 한 번만 하는 것

### 1. API 키 파일 만들기
`english-app` 폴더 안에 `.env` 파일을 새로 만들고 아래 내용을 입력하세요.
(메모장, TextEdit, VS Code 등 아무 편집기 사용)

```
ANTHROPIC_API_KEY=sk-ant-api03-여기에본인키붙여넣기
PORT=3001
```

### 2. 패키지 설치 (최초 1회)
터미널에서:
```
cd "영어 회화 앱/english-app"
npm install
```

---

## 매번 앱 실행할 때

터미널에서:
```
cd "영어 회화 앱/english-app"
npm run dev
```

그러면 아래 주소로 브라우저에서 앱이 열립니다:
👉 **http://localhost:5173**

---

## 핸드폰에서도 보려면
같은 Wi-Fi 연결 후, 터미널에 나오는 `Network:` 주소를 핸드폰 브라우저에 입력하세요.

---

## 앱 종료
터미널에서 Ctrl+C

